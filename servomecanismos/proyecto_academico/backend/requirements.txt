# Robot 2R — Backend de trayectorias y comunicación
# ===================================================

# === GUI ===
customtkinter>=5.2.0

# === Cálculo científico ===
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# === Comunicación serie (fallback si rust_comm no está disponible) ===
pyserial>=3.5

# === Rust bindings (compilar con: cd rust_comm && maturin develop --release) ===
# maturin>=1.4.0
# (rust_comm se instala vía maturin develop, no vía pip)

# === Utilidades ===
